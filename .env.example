DISCORD_TOKEN=
YOUTUBE_API_KEY=

# Optional: Discord user ID of the instance owner.
# This user bypasses all bot permission checks (e.g. can use /config without Manage Server).
INSTANCE_OWNER_ID=221701506561212416

############
# Optional #
############

# CACHE_LIMIT=2GB

# Optional: Register slash commands globally on the bot instead of per guild.
# REGISTER_COMMANDS_ON_BOT=true

# ENABLE_SPONSORBLOCK=true
# SPONSORBLOCK_TIMEOUT=5    # Delay (in mn) before retrying when SponsorBlock server are unreachable.
# YT_DLP_PATH=yt-dlp
# YT_DLP_AUTO_UPDATE=false  # Set to false to disable auto-update (enabled by default)
